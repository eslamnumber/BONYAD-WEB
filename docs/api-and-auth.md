# API and auth — mirroring the RN backend

## The `apiClient`

Lives in `src/lib/api-client.ts`. **Single instance.** Wraps `fetch` (the only file allowed to). Responsibilities:

- Resolve the base URL by runtime (see `src/lib/backend.ts` for `BACKEND_BASE_URL`): **server** (RSC / route handler) calls the backend directly; **browser** routes through the same-origin proxy `/api/proxy/*` (CSP `connect-src 'self'` forbids the browser hitting the backend directly). The `internal: true` option targets a same-origin Next route handler (e.g. `/api/auth/login`) instead.
- **Auth attach:** server-side, attach `Authorization: Bearer <token>` from the httpOnly cookie (caller passes `token`, typically via `getServerToken()`); browser calls leave `token` unset — the proxy reads the cookie and attaches the Bearer server-side, so the JWT never reaches browser JS.
- Throw `ApiError` on non-2xx with parsed body (`messageEn`, `messageAr`, `errorCode`, `fieldErrors`).
- **File uploads (multipart):** pass a `FormData` as `body` and `apiClient` skips JSON-encoding + omits the hand-set `Content-Type` so `fetch`/undici writes the `multipart/form-data` boundary itself. The proxy detects `multipart/form-data` on the incoming request, re-reads it via `req.formData()`, and re-streams it to the backend with the Bearer attached — same auth path as JSON. Used by `POST /chat/send-with-file` (chat image attachments); mirror this for any future upload rather than hitting the backend with a raw `fetch`.
- **Form-urlencoded bodies:** pass a `URLSearchParams` as `body` and `apiClient` passes it through untouched (like `FormData`) — `fetch`/undici writes the `application/x-www-form-urlencoded` Content-Type itself. Used by `POST /signatures` (contract e-sign), whose RN call site sends a urlencoded body. Build the `URLSearchParams` in the fetcher from a zod-validated request object.
- **401 handling:** the proxy (`src/app/api/proxy/[...path]/route.ts`) clears the session cookie on a 401 so the next navigation redirects to `/login`. _Automatic `/auth/refresh-token` retry is deferred — the RN app re-validates instead, so there is no verified refresh contract to mirror yet._
- Optionally validate the response body with a zod schema when callers pass one.
- **Streaming (`apiClient.stream`):** a POST that returns the raw `Response` instead of parsed JSON — the only way to consume a server-sent-events body (the SSE/line parsing then lives in the caller, not a `fetch`). Used by the Omdah SOW generation stream both server-side (route → Cloud Run) and client-side (hook → same-origin `/api/ai/chat/stream`). Throws `ApiError` on non-2xx so callers can fall back to the REST path.

> Not yet implemented (parity bullets kept for the roadmap): `ngrok-skip-browser-warning`, `Accept-Language` from `useLocaleStore`.

## Endpoints and types

- `src/config/endpoints.ts` re-creates the RN app's `API_ENDPOINTS` constant from `website-bonyad/src/config/api.ts` **exactly**. Keep in sync — never drift.
- Per-endpoint **request** schemas are strict zod (we control what we send); **response** shapes are TS types or permissive zod (backend can extend). See §Schema strategy.
- Fetchers in `features/X/api/` use `apiClient.get/post/put/delete` + the endpoint constant.
- **Two role-scoped project lists.** `GET /projects/my-assigned` (`PROJECTS.MY_ASSIGNED`) is the **technician's** assigned work (optional `?type=BIDDING|DIRECT_ASSIGNMENT`); `GET /projects/my` (`PROJECTS.MY`) is the **customer's own** projects — every status, no query params, returns `MyProject[]` (= `Project` + `assignedTechnicianName?` + `phases?`). The `/dashboard/projects` screen branches on `user.role` to pick which list to fetch (`useTechnicianProjects` vs `useMyProjects`).
- **The technician list is a MERGE, not a single endpoint.** `/projects/my-assigned` is **assigned-only** — a project appears there only **after** the customer accepts a bid (verified on dev: it returns APPROVED / CONTRACT*SIGNING / IN_PROGRESS, never the tech's \_pending* bids). A technician's **bid-phase** projects live exclusively in `GET /bids/my` (`BIDS.MY_BIDS`), which returns the tech's own bids carrying a flattened copy of each project. So `useTechnicianProjects` (`features/dashboard/api/get-my-bids.ts`) fetches **both** and merges them: each `/bids/my` row is folded into a `BID_RECEIVED` pseudo-`Project` (`myBidToProject`), then de-duped against the assigned list by `projectId` (assigned wins, so an accepted bid keeps its real status). Mirrors RN `useMyProjects.loadMyBids`. Because of this, **`useSubmitBid` / `useDeleteBid` / `useAcceptBid` invalidate the whole `['projects']` and `['bids']` key namespaces** — a fresh bid must immediately surface in (or drop out of) the Projects screen.
- **List and detail endpoints can have different shapes.** `GET /projects` (`PROJECTS.LIST`) returns a **flat** DTO (`serviceNameEn`, `userName`, `timeRequiredDays` top-level), but `GET /projects/:id` (`PROJECTS.DETAILS`) returns the **full entity wrapped** in `{ project, phases, regionId, regionName* }` with **nested** `project.user.name` and `project.service.name{En,Ar}`, a single `budget` (no min/max), and `projectType` as an enum (e.g. `"ALL"`). The detail fetcher (`features/dashboard/api/get-project.ts`) normalizes this back into the flat `ProjectDetail` the cards expect. When wiring any new detail endpoint, **curl it (or read the live response) before trusting the list shape** — they often diverge.
- **Contract signing (customer CONTRACT_SIGNING screen).** Two endpoints back the customer's contract-signing detail (`features/dashboard/api/`): `GET /contracts/project/:projectId` (`CONTRACTS.BY_PROJECT` → `Contract | null`; a **404 means "no contract yet", not an error**, and the body may be `{ contract }`-wrapped — mirrors RN `ContractService.getContractByProject`) supplies the "sent N ago" timestamp + status; `POST /signatures` (`SIGNATURES.CREATE`, **form-urlencoded** `projectId, technicianId, userEmail, technicianEmail, phaseIds(csv), language`; request validated by `signatureRequestSchema` — mirrors RN `usePhaseApprovalData.ts:170`) creates / resends the e-sign request.
- **Approve phases (customer APPROVED screen).** The upstream step — Figma "Dashboard-Singing Contract" (`CustomerApprovedDetail`) — is where the customer reviews the provider, picks a signing method (Email / Nafath / Absher), and approves the phases. The "Approve phases" CTA opens the review-phases modal whose confirm runs `POST /phases/project/:projectId/approve-all` (`PHASES.APPROVE_ALL`, empty JSON body → returns the new `projectStatus`; `features/dashboard/api/approve-all-phases.ts`), which transitions the project APPROVED/PHASE_PLANNING → CONTRACT_SIGNING (the only state that unlocks `POST /signatures`), then best-effort fires `POST /signatures` so the contract is emailed. The detail router (`assigned-project-detail.tsx`) then re-routes the customer to the CONTRACT_SIGNING screen above. All three method options are currently selectable but route through the same `/signatures` (email) flow; the dedicated Nafath/Absher endpoints (`/signatures/signature-nafath` · `…-absher`) are not yet wired on web.
- **Customer in-progress + per-phase payment (customer IN_PROGRESS screen).** Once execution starts, `CustomerInProgressDetail` (Figma 1547:1533, dispatched by `assigned-project-detail.tsx` for `customer + inProgress`) lists the phases with per-phase **Approve** actions. Approve opens the payment flow — choose **full phase** or **partial** (never the whole project; mirrors RN `PhasePaymentModal`) → review → **`POST /payments/create-checkout`** (`PAYMENT.CREATE_CHECKOUT`; `features/dashboard/api/create-checkout.ts`, strict `createCheckoutRequestSchema` request, permissive response normalised to `{ checkoutId ?? id ?? ndc, redirectUrl ?? shopperUrl, isMimic }`; mirrors RN `usePayment.ts:36` + `HyperPayService.ts:120`) → redirect to HyperPay's hosted page (or, in **mimic** mode, straight to the callback). HyperPay returns to **`/payment/callback`** (`app/payment/callback/page.tsx`, outside the `(app)` group → no sidebar) carrying `?type=phase&phaseId=&paymentType=&amount=&id=`; `PaymentCallbackView` verifies via **`GET /payments/status/:checkoutId`** (`PAYMENT.STATUS`; success = backend `paymentResult` or HyperPay code `000.000.000` / `000.100.1xx` / `000.3xx`, pending = `000.200.xxx`) then marks the phase paid via **`POST /phases/:phaseId/pay`** (`PHASES.PAY`, JSON `{ paymentType: FULL|PARTIAL, amount, paymentMethod, paymentReference, gatewayTransactionId }`; **non-fatal** — the charge already cleared even if this call fails; mirrors RN `PhaseService.ts:138` + `PaymentCallbackScreen.tsx`), and shows the success card (Figma 1553:8470) or a failed state, both routing back to `/dashboard/projects`. **No web HyperPay env var** — the gateway URL comes from the backend response; the customer/billing block is built from the auth-store user (billing uses RN defaults). The amount math + FULL/PARTIAL flag + callback context resolution live in pure, fully-tested `features/dashboard/lib/{phase-payment,checkout-request,payment-callback}.ts`.
- **Create-project wizard (customer).** The `/dashboard/projects/new` multi-step wizard writes through five endpoints, all mirrored from RN `useNewProjectView.ts`: `POST /projects/create` (`PROJECTS.CREATE`) is **multipart/form-data** — `title`, `description`, `serviceCategoryId`, `budget`+`budgetUnspecified`, `timeline`+`timeRequired` (days = weeks×7), `deliverables`, hardcoded `address/latitude/longitude`, `projectType` (**`ALL`** for bidding / `DIRECT_ASSIGNMENT` for direct), and `assignedTechnicianId`+`assignmentType` on direct. Optional phases are then POSTed **one at a time** to `POST /phases` (`PHASES.CREATE`, JSON `{ projectId, phaseNumber, title, description, timeSpentDays, moneySpent }`); per-phase failures are swallowed so they never undo a created project. The category dropdown reads `GET /services/categories` (`SERVICES.CATEGORIES` → `Service[]`) and the direct-assign picker reads `GET /users/technicians` (`USERS.TECHNICIANS_LIST` → `Technician[]`, optional `?serviceId=&searchQuery=`). The wizard's `useSubmitNewProject` chains create→phases; field reads/payload are copied verbatim from RN (the web's single category dropdown sends `serviceCategoryId` only, simplifying RN's category→subcategory two-level pick).
- **Customer dashboard search.** The search typeahead on the customer dashboard reads `GET /services` (`SERVICES.LIST` → `Service[]`, **all** services incl. subcategories) via `features/dashboard/api/get-all-services.ts` (`useAllServices`) — distinct from `SERVICES.CATEGORIES` (categories only) so the create-project picker keeps its category-only list. Matching/ranking is **client-side** (`features/dashboard/lib/dashboard-search.ts`: substring + highlight, a lean port of RN `utils/smartSearch`+`searchService`); "Suggested" = top categories by `displayOrder`. **Recent searches are client-only** (`features/dashboard/lib/recent-searches.ts`, localStorage) — there is no backend search-history endpoint. No technician fetch (the Figma dropdown matches services, not technician names).
- **Profile / account hub (`/dashboard/settings`).** The settings screen (`features/profile/`) reads the signed-in user's own profile via `GET /users/profile` (`USERS.PROFILE` → `UserProfile`, permissive type; `features/profile/api/get-my-profile.ts` (`useMyProfile`); mirrors RN `ProfileService.ts:399 getUserProfile`). The identity card merges the live profile with the hydrated session user (`resolveProfileIdentity` in `lib/identity.ts`) so it renders instantly and enriches with rating / Wathq once the fetch resolves. The hub is **role-aware** (technician-only rows — portfolio / services / account-type / **my subscriptions** — hidden for `USER`) and mostly **navigation** to profile sub-screens, most of which are placeholders for now (`ROUTES.DASHBOARD_SETTINGS_*`). It is the target of the sidebar gear nav item (both roles) and the customer profile-card menu (`SidebarSettingsMenu`).
- **Payment cards (`/dashboard/settings/cards`).** The profile hub's "Payment cards" row resolves to a real screen (`features/cards/`, the iOS `CardManagementView` — "unified" for customers and technicians, with role-aware copy: payouts vs payments). Five endpoints under `API_ENDPOINTS.CARDS` (mirroring the iOS `TechnicianCardService`, all `/user/cards*`): `GET /user/cards` (`CARDS.LIST` → `PaymentCard[]`, permissive — tolerates a bare array or `{ success, cards }`; `api/get-cards.ts`), `PUT /user/cards/:id/default` (`CARDS.SET_DEFAULT`), `DELETE /user/cards/:id` (`CARDS.DELETE`), and the **add-card** flow: `POST /user/cards/prepare` (`CARDS.PREPARE` → a 1 SAR HyperPay preauth checkout, response normalised to `{ checkoutId, redirectUrl ?? shopperUrl, isMimic }` exactly like `create-checkout`) → redirect to the hosted page → return to the cards page (`?registration=return` / `?id=`) → `POST /user/cards/complete` (`CARDS.COMPLETE`, strict `{ checkoutId }` body) which tokenises the card and reverses the charge. Every mutation throws on a `success: false` forwarded over HTTP 200 (`lib/card-response.ts` `assertCardOk`) and invalidates the `['cards']` key. The redirect/return plumbing is pure + tested in `lib/card-registration.ts` (`resolveCardRedirectTarget`, `resolveReturnCheckoutId`). Note: `prepare`/`complete` are not yet curl-verified on dev (auth-gated, no technician session); the permissive response types are the safeguard (CLAUDE rule 1).
- **My subscriptions (`/dashboard/settings/subscriptions`, technician-only).** The profile hub's "My subscriptions" row resolves to a real screen (`features/subscriptions/`, the iOS `SubscriptionManagementView`): current plan + price + dates + days-remaining, the weekly bid-usage meter, and cancel. Three endpoints under `API_ENDPOINTS.USERS` (mirroring RN `SubscriptionCard.tsx` + the iOS `SubscriptionModels`): `GET /users/subscription` (`USERS.SUBSCRIPTION` → `Subscription`, permissive — every field nullable; `api/get-subscription.ts`) where a **404 means "no active subscription"** and resolves to `null` (the empty state), not an error; `GET /users/subscription/bids` (`USERS.SUBSCRIPTION_BIDS` → `SubscriptionBids`, **non-critical** — `retry:false`, the bid-usage card is simply omitted on failure); and `DELETE /users/subscription` (`api/cancel-subscription.ts`, invalidates the `['subscription']` keys → screen flips to the empty state). Plan selection / subscribe + the HyperPay subscribe-payment path are **deferred**; the empty state's "Browse plans" CTA routes to the public pricing section (`/for-pros#packages`). Not yet curl-verified on dev (auth-gated, no technician session); permissive response types are the safeguard (CLAUDE rule 1).
- **Transactions & refunds (`/dashboard/payments`).** The profile hub's "Transactions" row (every role) resolves to a real screen (`features/dashboard/components/transactions/`, the iOS `TransactionsView`). Route lives at `/dashboard/payments` (**not** under `settings/`), `noindex`. Two tabs over three endpoints under `API_ENDPOINTS.PAYMENT` (mirroring RN `PaymentService.ts` + the iOS `PaymentTransactionService`): the **payment-history** tab reads `GET /payments/my-transactions` (`PAYMENT.MY_TRANSACTIONS`, paged `?status&type&page&size`; `api/get-my-transactions.ts` → `useMyTransactions(status?)`, a `useInfiniteQuery` keyed by the status filter so changing it restarts at page 0 — matches the iOS reset-on-filter), and the **refund-requests** tab reads `GET /payments/my-refund-requests` (`PAYMENT.MY_REFUND_REQUESTS`, paged; `api/get-my-refund-requests.ts`). Both responses are permissive — a Spring page **or** a bare array normalise through `lib/transaction-response.ts` `toPage`. A transaction card's "Request refund" (shown only when `canRequestRefund`) opens a dialog whose submit runs `POST /payments/transactions/:id/refund-request` (`PAYMENT.REQUEST_REFUND`, **strict** `requestRefundRequestSchema` body `{ reason }`, reason ≥ 10 chars; `api/request-refund.ts` → `useRequestRefund`, invalidates the `['payments','transactions']` + `['payments','refund-requests']` keys so the source card flips to "refund pending"). Status badges reuse the dashboard `--status-*` tokens (green = completed/approved, amber = pending, red = failed/rejected, blue = refunded). The four endpoint constants were **added to `endpoints.ts` in this change** (only `CREATE_CHECKOUT` / `STATUS` existed before). Not yet curl-verified on dev (auth-gated); permissive response types are the safeguard (CLAUDE rule 1).
- **Portfolio (`/dashboard/settings/portfolio`, technician-only).** The profile hub's "My portfolio" row resolves to a real screen (`features/portfolio/`). The **design is original** (Bonyad web identity, not the iOS layout); only the **backend contract** is taken from the iOS `PortfolioService`. The screen branches on the portfolio fetch (`api/get-portfolio.ts` → `Portfolio | null`): it tries **`GET /portfolios/me`** (`PORTFOLIO.ME`, the v2 primary) first, then falls back to legacy **`GET /portfolios/my`** (`PORTFOLIO.MY`) — exactly like `PortfolioService.checkPortfolioExists`. **A 404 _or_ a Spring "No static resource" routing miss** (this backend serves `/me` but **500s on `/my`** — the reported bug) means "no portfolio yet" → the create panel, not an error (`isMissingRoute` in `lib/portfolio-normalize.ts` detects both; the body is normalised — specialties array-or-comma-string + embedded projects). The projects gallery prefers `GET /portfolios/projects/my` but falls back to the projects embedded in the portfolio payload when that endpoint is empty/unavailable (the v2 `/me` body is lightweight). Core CRUD under `API_ENDPOINTS.PORTFOLIO` (all `/portfolios/*`): `POST /portfolios/create` (`CREATE`), `PATCH /portfolios/me` (`UPDATE`, partial basic-info edit), `GET /portfolios/projects/my` (`PROJECTS` → `PortfolioProject[]`, **404 / routing-miss → []**), `POST /portfolios/projects/add` (`ADD_PROJECT`), `PUT /portfolios/projects/:id` (`UPDATE_PROJECT`, **full replace** — the project form resends the combined kept-URLs + newly-uploaded `photos[]`), `DELETE /portfolios/projects/:id` (`DELETE_PROJECT`), and `POST /portfolios/projects/upload-photo` (`UPLOAD_PHOTO`, **multipart** field `file` → `{ photoUrl }`; the image picker uploads each new file then sends URLs to add/update). Requests strict-zod (`createPortfolioRequestSchema` / `updatePortfolioRequestSchema` / `projectInputSchema` — exported as `z.input` so `.default()` fields stay optional); responses permissive. Mutations invalidate `['portfolio','my']` + `['portfolio','projects']`. The heavy iOS **PDF / theme-customize / publish / share / QR / AI-fill pipeline (~20 endpoints) is intentionally deferred** — unverifiable on dev (auth-gated) and out of scope for the core "add your works" flow. Not yet curl-verified on dev; permissive response types are the safeguard (CLAUDE rule 1).

- **Support center (`/dashboard/settings/support`, both roles).** The profile hub's "Support center" row resolves to a real screen (`features/support/`). The **design is original** (Bonyad web identity); only the **backend contract** is mirrored from the iOS `SupportRequestService`. Three endpoints under `API_ENDPOINTS.SUPPORT`: **`POST /support/request`** (`REQUEST`, strict body `{ subject, description, category, priority }` via `createSupportRequestSchema` — the iOS `aiConversationHistory` is omitted, no web chatbot; `api/create-support-request.ts`), **`GET /support/my-requests`** (`MY_REQUESTS` → `SupportRequest[]`, permissive — tolerates a bare array or `{ requests }` / `{ data }`, sorted newest-first; `api/get-support-requests.ts`), and **`GET /support/requests/:requestId`** (`REQUEST_BY_ID` → `SupportRequestDetail`, fetched only while the detail modal is open; `api/get-support-request.ts`). `status`/`priority`/`category` are backend-controlled strings → never zod-enumerated; `resolveSupportStatus`/`resolveSupportPriority` (`lib/support-format.ts`) map them to a badge tone + i18n label with a neutral fallback. The screen mirrors the iOS **two-tab** support centre (a `SegmentedTabs` switch — original web design): **Conversations** (the requests above — client-side `RequestFilter` chips All/Pending/Active/Resolved; an `ASSIGNED`/`IN_PROGRESS` request with a `chatRoomRoomId` opens a **live conversation** — `GET /chat/room/:roomId/messages` (`api/get-conversation.ts`) + `POST /chat/send` (`api/send-conversation-message.ts`, body `{ roomId, receiverId, content }`) + realtime via the shared `mqttChat` singleton on topic `chat/room/{roomId}`, exactly like the messages feature; otherwise the read-only detail modal), and **Tickets** (threaded, admin-replied): server status filter `GET /support/tickets?status=` (`TICKETS`, `api/get-tickets.ts`), `GET /support/tickets/:id` incl. `messages[]` (`TICKET_BY_ID`), `POST /support/tickets` create (strict `{ subject, description, priority, categoryId?, subcategoryId? }`; pickers from public `GET /support/categories/hierarchy`, `CATEGORIES_HIERARCHY`), `POST /support/tickets/:id/messages` reply (`{ message, content }`), `PUT /support/tickets/:id/resolve` (`TICKET_RESOLVE`). Shared `SegmentedTabs` (`components/ui`), `StatusPill`, `MessageBubble`/`MessageComposer`. All four list states render on every list; per-endpoint tests ship for all 11 fetchers. File-attachment ticket create + the iOS AI-chatbot pre-step are deferred. Endpoint shapes mirrored from the iOS/RN production contract; **not curl-verified on dev** (auth-gated, no session token) — permissive response types are the safeguard (CLAUDE rule 1).

- **Feedback / الملاحظات (`/dashboard/settings/feedback`, both roles).** The profile hub's "Feedback" row resolves to a real screen (`features/feedback/`). The **design is original** (Bonyad web identity); only the **backend contract** is from the product team (iOS-native feature, absent from the RN call-site index → the supplied shapes are authoritative). Two endpoints under `API_ENDPOINTS.APP_FEEDBACK`: **`POST /app-feedback`** (`SUBMIT`, strict body `{ category, subject, message, attachments? }` via `createFeedbackSchema` — `subject` sent as `null` when blank, `attachments` omitted entirely until upload is wired; returns the created `AppFeedback`; `api/create-feedback.ts`) and **`GET /app-feedback/mine`** (`MINE` → `AppFeedback[]`, permissive — tolerates a bare array or a `{ success, count, feedback }` / `{ data }` envelope, sorted newest-first; `api/get-my-feedback.ts`). `category`/`status` are backend-controlled strings → never zod-enumerated; `resolveFeedbackStatus` maps `NEW`/`REVIEWED`/`RESOLVED` to a badge tone (`status-offer`/`status-progress`/`status-approved`) + i18n label with a neutral fallback, `resolveFeedbackCategoryKey` falls unknown categories back to "other" (`lib/feedback-format.ts`). The list renders all four states (pending skeletons / error+retry / empty+CTA / cards with optional admin "team note"); compose is a modal (`SubmitFeedbackModal`) → message required + 1000-char cap with a live counter. **Direction override:** the screen + modal render in the conventional mapping (en→ltr, ar→rtl) via `dir={conventionalDirection(locale)}` (shared `src/types/locale.ts`) — a scoped exception to the inverted `LOCALE_DIRECTION`, per product request (same pattern as `features/support`). Per-endpoint tests ship for both fetchers. Attachment upload + pagination are deferred. Not curl-verified on dev (auth-gated, no session token) — permissive response types are the safeguard (CLAUDE rule 1).

- **Refer & earn (`/dashboard/settings/referral`, both roles).** The profile hub's "Refer & earn" row resolves to a real screen (`features/referral/`). The **design is original** (Bonyad web identity — the brand-blue reward-wallet hero, funnel tiles, grouped invitation list); only the **backend contract** is mirrored from the iOS `ReferralAPIService`/`ReferralView`. Four endpoints under `API_ENDPOINTS.USERS`: **`POST /users/me/referral/invite`** (`REFERRAL_INVITE`, strict body `{ phoneNumber }` via `inviteRequestSchema` — re-normalised to the national `5XXXXXXXX` form by the shared `saudi-phone` util; `api/send-invite.ts`), **`GET /users/me/referrals/stats`** (`REFERRAL_STATS` → `ReferralStats`, permissive; the funnel + reward `wallet_balance` + `next_tier_at`/`next_tier_reward`), **`GET /users/me/referrals`** (`REFERRALS` → `{ invitations[], referrals[] }`, permissive; `lib/referral-groups.ts` buckets it into joined / pending-signup / expired exactly like the iOS screen, synthesizing a row for a `SIGNED_UP` invite with no referral yet), and **`GET /users/me/wallet`** (`WALLET` → `ReferralWallet`, the SAR reward balance). **A 404 on any GET means "no activity yet"** and folds to an empty value (stats `{}`, list `{}`, wallet `null`) — never an error; the hero falls back to `stats.wallet_balance`, then 0. The invite endpoint signals failure either with `success:false` over HTTP 200 **or** a 4xx — both carry a snake_case `error_code` (`RATE_LIMIT`/`ALREADY_USER`/`SELF_REFERRAL`/`INVALID_PHONE`), read off `ApiError.body` and surfaced as a typed `InviteError` whose `.code` maps to a localized message (`inviteErrorMessageKey`). On success the mutation invalidates the `['referral',*]` keys. The iOS **contacts picker has no web analogue** (no browser Contacts API), so the SMS-by-phone manual invite is the single invite path. These four constants are **net-new** in `endpoints.ts` (the iOS "referrals" project-supervision endpoints are a different concept and are not these). Not curl-verified on dev (auth-gated, no session token); permissive response types are the safeguard (CLAUDE rule 1).

- **Omdah AI project creation (`/dashboard/projects/create/ai`).** The AI path (after the local interview) generates a Scope-of-Work, lets the user refine it, then publishes a real project. **Three backends** (all verified live on dev): the conversational chatbot (Google Cloud Run) and the SOW-refine service (AWS) live on **foreign hosts** (`src/config/ai-hosts.ts`, hardcoded like `api-environments.ts`) and are reached through **same-origin Next route handlers** under `src/app/api/ai/{chat,chat/stream,refine,health}/route.ts` (the browser can't call them cross-origin under CSP `connect-src 'self'`); the create/draft/attachment endpoints are on the **main backend** and go through the standard `/api/proxy/*`. **Two endpoint sets in `endpoints.ts`:** `API_ENDPOINTS.AI.*` = the foreign-host paths used by the route handlers (`apiClient.*` with `baseUrl: AI_HOSTS.*`); `AI_INTERNAL_ROUTES.*` = the same-origin routes the client fetchers call with `internal: true`. Don't conflate them. SOW streaming uses **`apiClient.stream`** (returns the raw `Response` for SSE — the only `fetch` that isn't `apiClient.get/post`; the SSE line-parsing lives in `features/dashboard/api/ai/parse-sse.ts`, not a `fetch`). **Gotcha:** the chatbot needs the hard-coded Arabic GATHER trigger (`"أبي أنشئ مشروع"`, `prewarm.ts`) sent on the conversationId **before** the gather payload, else it replies "tell me more" instead of a SOW (trigger phrases are Arabic regardless of UI locale; DONE trigger `"نعم، انشر المشروع"` on publish). **Graceful degradation** (mirrors iOS): stream → REST `/api/chat` → local-SOW (`build-local-sow.ts`); never hard-blocks. Publish (`run-publish.ts`): `GET /services` match (`match-service.ts` — `serviceId` is **mandatory**, a null match surfaces the "couldn't match a service" error) → `POST /v1/projects/from-ai` (the hard gate) → best-effort `POST /phases` (per milestone) + `POST /v1/projects/:id/attachments` (multipart) + `PUT /v1/ai/draft` (fire-and-forget). Every `features/dashboard/api/ai/*.ts` ships a sibling test. The publish location step uses **Google Maps** (`@react-google-maps/api`, key in `env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`, CSP-allow-listed — see `deployment-and-env.md` + `security-headers.md`). The flow renders in the **conventional** direction override (en→ltr, ar→rtl) per product request.
- **AI Scope-of-Work on the status screens.** AI-created projects carry the SOW back on the project object (mirrors the iOS `BackendProject`): `aiGenerated`/`hasSow` flags + `sowJsonSnapshot` (full SOW JSON, source of truth) + flat `sow*` columns — modeled (permissive) on the `Project` type in `schemas/project.ts`, and preserved through `normalizeProjectDetail` (it spreads the whole raw body). The shared **`ProjectSowPanel`** (`components/project-sow/`, see `components.md`) parses them (`parseProjectSow` — snapshot-first, flat-column fallback) and reuses the create-flow `SowSections` to show every AI-only output read-only on **every lifecycle screen** — wired once in `assigned-project-detail.tsx` (all statuses) + inside `job-offer-detail.tsx` (the bid view / `/dashboard/job-offers/[id]`) — gated on a SOW being present (manual projects render nothing).

```ts
// features/projects/api/get-projects.ts — typed response, no runtime schema parse
import { apiClient } from '@/lib/api-client';
import { API_ENDPOINTS } from '@/config/endpoints';
import type { Project } from '../schemas/project.schema';

export async function getProjects(): Promise<Project[]> {
  return apiClient.get<Project[]>(API_ENDPOINTS.PROJECTS.LIST);
}
```

## Schema strategy (read this before adding any endpoint)

The backend is shared with the RN app. It may add fields, return enum values we haven't seen (`role: 'ADMIN'` on a USER/TECHNICIAN endpoint), or return slightly different shapes per case. Strict zod parsing on the response side turns every backend evolution into a silent client-side crash that surfaces as a misleading "Something went wrong" — the `ZodError` isn't an `ApiError`, so the UI falls through to the generic fallback with no 4xx log to diagnose from.

**The rule:** validate what you SEND, trust-but-narrow what you RECEIVE.

- **Request body** — strict zod (`loginRequestSchema.parse(...)`). Catches typos at compile time and at boot.
- **Response body** — a plain TS `type` (preferred), or a permissive zod schema where every field is `.optional()` and every backend-controlled string is `z.string()` not `z.enum([...])`. Pass `apiClient.post<ResponseType>(...)` WITHOUT the `schema:` arg unless the response shape is truly closed (e.g. a `count: number` ping). Narrow at the consumer with helpers like `pickRole(raw, fallback)`.
- **Error envelope** — `{ messageEn, messageAr, errorCode }` is parsed into `ApiError` by the `apiClient`; never re-parse it.

## Auth flow (mirrors RN)

1. **Login** — `POST /auth/login` → `{ token, user, requiresPasswordChange? }` on success, or a pending-verification response (see below). Persist token in an `httpOnly` cookie via a Next.js route handler `app/api/auth/login/route.ts` so SSR can read it.
2. **Token validation on boot** — `POST /auth/validate-token` with the cookie token. On success, hydrate `useAuthStore` with the user.
3. **Refresh** — `apiClient` calls `POST /auth/refresh-token` once on 401, retries original request.
4. **Logout** — every sign-out trigger (sidebar account menu, profile settings row) first opens `LogoutConfirmModal` (`features/auth/components/logout-confirm-modal.tsx`, exported from `@/features/auth`); only its confirm button runs `useLogout`, which clears the cookie + `useAuthStore.clearSession()` + `queryClient.clear()` + redirects to `/login`. Triggers just open the dialog — they never call `logout()` directly.
5. **Protected routes** — a middleware in `src/middleware.ts` (must be in `src/`, not the repo root — see [security-headers.md](security-headers.md)) checks the auth cookie for the protected prefixes (`/dashboard`, `/app`). Unauthenticated requests redirect to `/login?next=<path>`.
6. **Role gating at the component level** — use `<RoleGate roles={['TECHNICIAN']}>` from `features/auth/`. Don't sprinkle role checks inside random child components.

## Backend response contract (mirrored from the RN app)

The backend at `bonyad-app-…run.app/api` returns **the same shapes** for the web app and the RN app — both mobile and web hit the same Cloud Run service. Mirror these exactly; never invent fields.

### `POST /auth/login` request

```json
{
  "phoneNumber": "501234567",
  "password": "...",
  "role": "USER" | "TECHNICIAN",
  "fcmToken": "no-token"
}
```

- `phoneNumber` — digits only, **no leading zero** (`5XXXXXXXX`, 9 digits for Saudi mobile). UI-entered Arabic numerals must be normalized to Western digits and the leading `0` stripped before sending.
- `fcmToken` — pass the literal string `"no-token"` on web (the web app does not have FCM). Backend requires the field to be present.

### Success response (200 OK)

```json
{
  "token": "...",
  "user": { "id": 123, "role": "USER", "deviceToken": "...", "forcePasswordChange": false },
  "requiresPasswordChange": false
}
```

Read `userId` as `data.user?.id ?? data.userId ?? 0` and `role` as `data.user?.role ?? data.role ?? <requested role>` — older entries on the backend return the bare `userId`/`role` at the top level.

### Pending-verification response — two valid shapes

The user signed up but never verified their OTP. The backend signals this in **two different ways** that both mean "redirect to `/verify-otp`":

1. **200 OK with no token**: body contains `message` whose lowercased text includes `"pending verification"` or `"otp sent"`.
2. **400 Bad Request** with body `{ messageEn, messageAr, errorCode: "USER_ALREADY_EXISTS_PENDING" }`.

`features/auth/api/login.ts` normalises both into `{ kind: 'pending', phoneNumber, role }` so the form can `router.push('/verify-otp?phone=…&role=…&source=login')` from a single branch.

### Forgot-password flow ≠ the `/verify-otp` registration flow

`/verify-otp` (and the `VERIFY_OTP` / `RESEND_OTP` endpoints) belongs to **registration / login-pending** only. The **forgot-password** flow is a separate two-screen sequence that never calls `verify-otp`:

1. `/forgot-password` — phone **+ role toggle on the same screen** → `POST /auth/forgot-password` sends the OTP, then `router.push('/reset-password?phone=…&role=…')`.
2. `/reset-password` — OTP **and** the new password are entered together and sent in one `POST /auth/reset-password` (`{ phoneNumber, role, otpCode, newPassword, confirmPassword }`); the backend verifies the code as part of the reset. Resend uses `FORGOT_PASSWORD_RESEND` (`/auth/forgot-password/resend`).

This mirrors the RN `ForgotPasswordScreen` → `ForgotPasswordOTPScreen` (which calls `resetPassword` directly). Do **not** insert a `verify-otp` call into the forgot flow — verifying the OTP separately can consume it and break the subsequent reset.

### Error response (400 / 401 / 5xx)

```json
{
  "messageEn": "Account not found. Please check your phone number and role, or register first.",
  "messageAr": "الحساب غير موجود. يرجى التحقق من رقم الهاتف والدور، أو التسجيل أولاً.",
  "errorCode": "ACCOUNT_NOT_FOUND"
}
```

- **The backend localises for us.** Use `ApiError#localizedMessage(i18n.language)` from `src/lib/api-client.ts` to pick `messageAr` or `messageEn` and display directly — do NOT wrap in `t('errors.…')` (this is a deliberate exception to the "never display raw backend strings" rule because the strings are already locale-correct).
- Map `err.errorCode` to special UI flows where needed (e.g. `USER_ALREADY_EXISTS_PENDING` → OTP screen). For everything else, render the localised message verbatim.

The same contract applies to `register`, `forgot-password`, `verify-otp`, `resend-otp`, `reset-password`, and `validate-token`. Mirror their request bodies from `website-bonyad/src/screens/auth/` rather than guessing field names.

## Token storage

- The token lives in an `httpOnly` cookie set by the Next.js route handler. **Not in localStorage, not in JS-readable storage.**
- The cookie is `Secure`, `SameSite=Lax`, path `/`.
- On same-origin API calls, the cookie is sent automatically; the `apiClient` does not need to read it.
- For cross-origin API calls (the backend at `bonyad-app-…run.app`), the `apiClient` proxies through a Next route handler `app/api/proxy/[...path]/route.ts` that attaches the token server-side.

## Rules

1. **No `fetch` call lives outside `apiClient`.** Grep for `fetch(` — must only match `lib/api-client.ts`.
2. **No endpoint string literal lives outside `config/endpoints.ts`.** Grep for `'/auth/'`, `'/projects'` — must only match that file.
3. **Strict request schemas, permissive response types.** Requests use a zod schema parsed at the call site. Responses use a TS `type` (or `.optional()`-everywhere zod) — never `z.enum` on a backend-controlled string field. See §Schema strategy for the reasoning.
4. **Pagination and filters are typed.** A `ProjectFilters` type lives next to the fetcher.
5. **Mutations return the updated entity** when the backend does — don't refetch by default if the response already has the data; just call `queryClient.setQueryData` in `onSuccess`.
6. **Idempotency keys** for any mutation that writes money (payments, payouts) — generate a UUID client-side and pass as `Idempotency-Key` header.
7. **Surface localised backend errors verbatim.** On `ApiError`, use `err.localizedMessage(i18n.language)` and only fall back to `t('errors.generic')` when that returns undefined. Mapping `err.errorCode` to a special UX flow (e.g. OTP redirect on `USER_ALREADY_EXISTS_PENDING`) lives in the feature, not in `apiClient`.

## Before adding any new endpoint integration

The backend is **shared with the RN app** and shouldn't change for the web app's sake unless absolutely necessary.

These steps are **one vertical endpoint slice** — schema + fetcher + hook + test + MSW shipped and gated together, one endpoint per sub-phase ([task-workflow.md](task-workflow.md) §Backend integration). Do not split them across separate "all schemas" / "all hooks" passes.

1. **Find the RN call site via the index — do not grep blind.** Open [`rn-call-site-index.md`](rn-call-site-index.md), look up the endpoint constant, and `Read` ONLY the mapped `file:line` range (`offset`/`limit` ≈ ±15 lines). Call sites are ranked **`services/` first** — that is where the request body is built and the response is read (`data.user?.id ?? data.userId`), NOT `screens/` (screens just trigger the flow). Copy the **request body field names** and the **response field reads** from there; do not guess names, and do not read the whole file. Only if the constant is missing from the index (or marked _(defined, no RN usage)_) do you grep the legacy tree — then run `pnpm gen:rn-index` so the map covers it. The index is generated and, because `website-bonyad/` is frozen, never drifts.
2. **Curl the endpoint** for at least three responses (success, common 4xx, edge case). Confirm the actual JSON shape before writing any code. This is the only reliable way to learn which fields are top-level vs nested and which enum values can appear.
3. **Confirm the endpoint exists in `website-bonyad/src/config/api.ts`.** If you need to add a new one, discuss with the backend team and add it to **both** `src/config/endpoints.ts` and `website-bonyad/src/config/api.ts` so the RN app inherits it.
4. **Write a strict request schema** in `features/<f>/schemas/`. Parse it at the call site.
5. **Write a permissive response TS type** in the same schemas file (no `z.enum` on backend-controlled fields; mark everything optional that isn't guaranteed by the curl evidence).
6. **Handle the error envelope at the form layer** — `ApiError#localizedMessage` for display, `err.errorCode` for branch decisions. Don't swallow the message into a generic fallback unless `localizedMessage` returns undefined.
7. **Ship a sibling `*.test.ts` for the fetcher in the same PR.** Per CLAUDE.md hard rule 1, every new `features/X/api/<endpoint>.ts` requires `<endpoint>.test.ts` covering: (a) happy path with captured request body matching the RN call site, (b) one representative 4xx that surfaces as `ApiError` with `messageEn` / `messageAr` / `errorCode` populated, (c) every special-state branch the fetcher carries (pending / `errorCode`-driven redirect / unfamiliar enum values / phone-normalisation). Form tests don't satisfy this — they're integration, not branching. See [testing.md](testing.md) §Per-endpoint tests for the MSW capture pattern.

## Realtime chat (MQTT)

The chat feature is **REST source-of-truth + an MQTT live layer**. The REST endpoints mirror `website-bonyad/src/config/api.ts` and live under `API_ENDPOINTS.CHAT`:

| Constant              | Path                                  | Use                                                              |
| --------------------- | ------------------------------------- | ---------------------------------------------------------------- |
| `CHAT.MY_CHATS`       | `/chat/my-chats`                      | conversation list (`ChatRoom[]`, possibly wrapped in `{ data }`) |
| `CHAT.MESSAGES`       | `/chat/room/:roomId/messages`         | message history for a room                                       |
| `CHAT.SEND`           | `/chat/send`                          | send a text message                                              |
| `CHAT.SEND_WITH_FILE` | `/chat/send-with-file`                | send with an attachment                                          |
| `CHAT.MARK_READ`      | `/chat/messages/:messageId/mark-read` | mark one message read                                            |
| `CHAT.MARK_ALL_READ`  | `/chat/rooms/:roomId/mark-all-read`   | mark a room read                                                 |

**Live transport** — `src/lib/mqtt-chat.ts` is a browser MQTT-over-WebSocket singleton (`mqtt.js`, lazy-imported). Broker URL is `NEXT_PUBLIC_MQTT_BROKER_URL` (default `wss://admin.bonyad-hub.com/mqtt`); the broker origin is allow-listed in the CSP `connect-src` ([security-headers.md](security-headers.md)). Topics mirror RN: `chat/user/{userId}` (cross-room notifications), `chat/room/{roomId}` (messages), `chat/room/{roomId}/read` (receipts), `chat/room/{roomId}/typing`. The lib is **payload-agnostic transport** — it dispatches parsed JSON to per-topic handlers; the messages feature owns `ChatMessage` and the topic strings.

**Broker credential** — the broker authenticates a connection with the **session JWT as its username**, but that JWT lives in an httpOnly cookie the browser can't read. The same-origin route handler `GET /api/chat/mqtt-credentials` (`INTERNAL_API.CHAT_MQTT_CREDENTIALS`) reads the cookie server-side and mints the token for the client (`Cache-Control: no-store`). This is a deliberate, scoped widening of the httpOnly boundary — accepted so realtime chat can run client-side.

**Graceful fallback** — every MQTT failure path (load error, broker down, credential denied) resolves to "not connected"; the chat still loads/sends over REST and falls back to refetch/polling. MQTT is an enhancement, never a hard dependency.
