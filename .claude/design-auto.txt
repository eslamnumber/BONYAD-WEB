[design · auto] DESIGN / UI / FIGMA / RTL TASK — do NOT treat this as a surgical smart-next edit. FIRST invoke the `bonyad-production-design` skill (Skill tool); it loads the full workflow as one unit. Then:
• Token discipline still applies: map-lookup before searching (docs/repo-map.md, rn-/ios-call-site-index.md), no blind Explore/grep, narrow offset/limit reads, never node_modules/.next/build/.env*.
• Phase 0 = plan + STOP (files · endpoints · role(s) · loading/empty/error/success states · risk). Backend-driven content is fetched (TanStack hook in features/X/api + sibling *.test.ts), never hardcoded; render every state.
• RTL/LTR = MATCH THE APP, never re-derive from "standard" RTL. This app is INVERTED (en→rtl, ar→ltr) via LOCALE_DIRECTION — never read `locale === 'ar'`. Logical utilities only (ms/me/ps/pe/start/end, text-start/-end); default `-end` for content. Any direction-sensitive element (progress/meter fill, slider, edge-pinned overlay, carousel, icon+stat row) is COPIED verbatim from the existing one (grep project-progress-card / JobOffersSection / job-offer-item) — hand-rolling it is the #1 recurring defect. No dir="auto" on static labels (only dynamic/punctuated copy, paired with text-start). Chevrons: one icon + flip class (ltr:-scale-x-100 forward, rtl:-scale-x-100 inward). See docs/i18n-and-rtl.md.
• Tokens only (no hex/rgb/font-family literal) · every string via t() added to BOTH en.json + ar.json · Server-Component default · file ≤200 / fn ≤50 · WCAG AA · reuse src/components/* before building new.
• Skip every Figma-FETCH step unless the user handed over a Figma link to import.
• END with the mandatory gate — paste REAL output of `pnpm lint` + `pnpm typecheck` + `pnpm test` + `pnpm build`, and verify 5 axes (RTL+LTR · dark · responsive 320→1920 · a11y AA · tokens/i18n). Reason explicitly for any axis you can't observe (authed screens have no preview session).
Full workflow: the `bonyad-production-design` skill + docs/task-workflow.md + docs/i18n-and-rtl.md.
